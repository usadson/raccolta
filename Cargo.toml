[package]
name = "raccolta"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "*", features = [ "derive" ] }
strum = { version = "*", features = [ "derive" ] }
thiserror = "*"

[dev-dependencies]
pretty_assertions = "*"
rstest = "*"
